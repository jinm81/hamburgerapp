<li>
	{{name}}

	<button class="change-devour" data-id="{{id}}" data-newdevour="{{devours}}">
		{{#if devours}}SLEEP TIME!{{else}}WAKE UP!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
